import styled from '@emotion/styled'

export const StyledGalleryWidget = styled.div`
    .header {
        display: flex;
        flex-direction: column;
        column-gap: 0.5rem;
        padding-bottom: 1rem;
    }
`
